import React from 'react';

import { View, StyleSheet, Text } from 'react-native';

const AppLoading = () =>{
    return(
        <View>
            <Text style={{flex:1, justifyContent:'center', alignSelf:'center'}}>Loading</Text>
        </View>
    )
}

export default AppLoading;